# Environment Variables Configuration for yaml-diffs API
# Copy this file to .env for local development (do not commit .env to git)

# ============================================================================
# Server Configuration
# ============================================================================

# Port to bind the server to
# Railway sets this automatically - do not override in Railway deployment
# For local development, defaults to 8000 if not set
PORT=8000

# Host to bind the server to
# Railway requires 0.0.0.0 for networking - this is the default
# Do not change this for Railway deployment
HOST=0.0.0.0

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Recommended: INFO for production, DEBUG for development
LOG_LEVEL=INFO

# ============================================================================
# CORS Configuration
# ============================================================================

# Comma-separated list of allowed CORS origins
# Default: empty list (no CORS) - most secure
# For local development with frontend, set to: http://localhost:3000,http://localhost:5173
# For production, specify exact domains: https://yourdomain.com,https://app.yourdomain.com
# WARNING: Setting to "*" allows all origins - insecure for production!
CORS_ORIGINS=

# Allow credentials in CORS requests
# Default: true
CORS_ALLOW_CREDENTIALS=true

# Comma-separated list of allowed HTTP methods
# Default: * (all methods)
# For production, consider restricting: GET,POST,OPTIONS
CORS_ALLOW_METHODS=*

# Comma-separated list of allowed headers
# Default: * (all headers)
# For production, consider restricting to specific headers
CORS_ALLOW_HEADERS=*

# ============================================================================
# Application Metadata
# ============================================================================

# Application name
# Default: yaml-diffs API
APP_NAME=yaml-diffs API

# Application version
# Default: 0.1.0
# This should match the version in src/yaml_diffs/__version__.py
APP_VERSION=0.1.0

# ============================================================================
# Railway-Specific Notes
# ============================================================================
# Railway automatically sets the following:
# - PORT: Set by Railway (do not override)
# - RAILWAY_ENVIRONMENT: Set to "production" or "development"
# - RAILWAY_PROJECT_ID: Project identifier
# - RAILWAY_SERVICE_ID: Service identifier
#
# For Railway deployment:
# 1. Set CORS_ORIGINS to your production domain(s)
# 2. Set LOG_LEVEL to INFO or WARNING for production
# 3. Do not set PORT (Railway sets it automatically)
# 4. Configure other variables as needed in Railway dashboard
